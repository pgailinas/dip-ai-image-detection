## Datasets

The `datasets/` folder contains dataset-specific configuration modules (e.g., COCO, DiffusionDB, ImageNet). 
Each module defines metadata file names, labels, and dataset-specific handling used throughout the pipeline.
